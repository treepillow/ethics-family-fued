import React from "react";
import ReactDOM from "react-dom/client";
import FamilyFeud from "./FamilyFued.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FamilyFeud />
  </React.StrictMode>
);